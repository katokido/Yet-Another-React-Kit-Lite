import React, { Component } from 'react'
import { hot } from 'react-hot-loader'

class App extends Component {
  render() {
    return (
      <h1>Hello, world. React Project demo</h1>
    )
  }
}

export default hot(module)(App)
